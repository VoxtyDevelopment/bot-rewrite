-- YOU ARE NOT REQUIRED TO IMPORT THIS FILE THE BOT WILL DO IT ON STARTUP THIS IS FOR THE BOT TO GET THE TABLES DO NOT EDIT THIS FILE

CREATE TABLE IF NOT EXISTS muterecords (
    id INT AUTO_INCREMENT PRIMARY KEY,
    discId VARCHAR(255),
    modId VARCHAR(255),
    muteReason TEXT,
    muteDate DATETIME NOT NULL
);

CREATE TABLE IF NOT EXISTS activemutes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    guildId VARCHAR(255) NOT NULL,
    discId VARCHAR(255) NOT NULL,
    muteReason TEXT DEFAULT NULL,
    roles VARCHAR(255) NOT NULL,
    muteChannel VARCHAR(255) NOT NULL
);

CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL DEFAULT '',
    discId VARCHAR(255) NOT NULL,
    webId VARCHAR(255),
    ts3 VARCHAR(255) NOT NULL,
    steamHex VARCHAR(255) NOT NULL
);

CREATE TABLE IF NOT EXISTS bans (
    id INT AUTO_INCREMENT PRIMARY KEY,
    discId VARCHAR(255) NOT NULL,
    steamHex VARCHAR(255),
    reason VARCHAR(255)
);

CREATE TABLE IF NOT EXISTS aop (
    id INT AUTO_INCREMENT PRIMARY KEY,
    aop VARCHAR(255) NOT NULL,
    setBy VARCHAR(255) NOT NULL,
    serverName VARCHAR(255) NOT NULL
);